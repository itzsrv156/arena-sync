// API Service for Stadium Management System
const API_BASE_URL = '/api';

export const fetchStadiumData = async () => {
  // Placeholder API call
  console.log(`Fetching from ${API_BASE_URL}...`);
  return { status: 'ok', data: [] };
};
